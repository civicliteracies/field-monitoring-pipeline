"""The Fieldbook pipeline: the code that watches, captures, reads, and publishes.

This package holds every module that makes up the funding monitor. Nothing lives
here directly; each step of the run is its own file, and `docs/ARCHITECTURE.md`
maps what each one does in plain language.
"""
